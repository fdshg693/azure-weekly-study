# これをコピーして .env を作る（.env は GITIGNORE 済み）。
#   pwsh:  Copy-Item .env.example .env
# ローカル学習用なのでパスワードはダミーで良い。

POSTGRES_USER=app
POSTGRES_PASSWORD=devpassword
POSTGRES_DB=lab

# ホスト側で待ち受けるポート（5432 が既に埋まっていれば 15432 などに変える）。
HOST_PORT=5432
