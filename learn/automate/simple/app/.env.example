# ローカルで `python worker.py` を試すとき用の雛形。
# 実機 (Container Apps Job) では Bicep の template.containers[].env から注入されるので不要。
# 使うときは `.env` にコピーして編集する（.env は Git 管理外）。

JOB_MESSAGE=hello from local run
WORK_SECONDS=3
# true にすると exit 1 で失敗する（リトライ挙動の確認用）
FAIL_JOB=false
